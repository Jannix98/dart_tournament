<UserControl x:Class="DartTournament.WPF.Controls.GameTreeControl.MatchControl.MatchControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DartTournament.WPF.Controls.GameTreeControl.MatchControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Height="100" Width="200">
    <UserControl.Resources>
        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="4"/>
        </Style>
    </UserControl.Resources>
    <Border BorderBrush="Gray" BorderThickness="1" Margin="4" Padding="4" CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="{Binding Match.Team1Name, RelativeSource={RelativeSource AncestorType=UserControl}}" />
            
            <Separator Grid.Row="1" Margin="0,4" Height="1" Background="Gray"/>

            <TextBlock Grid.Row="2" Text="{Binding Match.Team2Name, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        </Grid>
    </Border>
</UserControl>
