<UserControl x:Class="DartTournament.WPF.Controls.GameNavigationRail.GameNavigationRail"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DartTournament.WPF.Controls.GameNavigationRail"
             mc:Ignorable="d" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:resx="clr-namespace:DartTournament.WPF.Resources"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <local:GameNavigationRailVM/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <Style TargetType="materialDesign:PackIcon">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                         Value="True">
                    <Setter Property="Foreground"
                        Value="White" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <!-- surface container -->
        <ListBox Grid.Column="0" ItemsSource="{Binding Items}" Background="#F3EDF7" 
                         SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
            <!--<ListBox.GroupStyle>
                <GroupStyle>
                    <GroupStyle.Panel>
                        <ItemsPanelTemplate>
                            <StackPanel />
                        </ItemsPanelTemplate>
                    </GroupStyle.Panel>
                    <GroupStyle.ContainerStyle>
                        <Style TargetType="GroupItem">
                            <Setter Property="Margin" Value="0,10,0,0"/>
                        </Style>
                    </GroupStyle.ContainerStyle>
                </GroupStyle>
            </ListBox.GroupStyle>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem"
                               BasedOn="{StaticResource MaterialDesign3.NavigationDrawerPrimaryListBoxItem}">
                    <Setter Property="materialDesign:NavigationRailAssist.SelectedIcon"
                                    Value="{Binding SelectedIcon}" />
                    <Setter Property="materialDesign:NavigationRailAssist.UnselectedIcon"
                                    Value="{Binding UnselectedIcon}" />
                </Style>
            </ListBox.ItemContainerStyle>-->
            <ListBox.Resources>
                <DataTemplate DataType="{x:Type local:GameNavigationItem}">
                    <TextBlock Text="{Binding Text}"
                                       FontSize="16" />
                </DataTemplate>
            </ListBox.Resources>
        </ListBox>

        <!-- Sidebar ListBox -->

        <!-- Content Display -->
        <Grid Grid.Column="1" Background="#F7F2FA">
            <!-- surface container low -->
            <!--<Label VerticalAlignment="Center" HorizontalAlignment="Center" Content="Please create or load a game!"/>-->

            <ContentControl Grid.Column="1"
                        Content="{Binding SelectedItem.Content}" />
        </Grid>

    </Grid>
</UserControl>
