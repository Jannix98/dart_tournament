<UserControl x:Class="DartTournament.WPF.Controls.LoadGame.LoadGameDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DartTournament.WPF.Controls.LoadGame"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:valueConverter="clr-namespace:DartTournament.WPF.ValueConverter"
             xmlns:resources="clr-namespace:DartTournament.WPF.Resources"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="450">
    
    <UserControl.DataContext>
        <local:LoadGameDialogVM />
    </UserControl.DataContext>

    <UserControl.Resources>
        <valueConverter:CountToVisibilityConverter x:Key="CountToVis"/>
        <valueConverter:BoolConverter x:Key="BoolConverter"/>
        <valueConverter:NotNullToBooleanConverter x:Key="NotNullToBool"/>
    </UserControl.Resources>

    <materialDesign:Card materialDesign:ElevationAssist.Elevation="Dp8" 
                         MaxWidth="450"
                         MinHeight="500"
                         MinWidth="400"
                         MaxHeight="600">
        <Grid Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0"
                          Margin="16,16,0,16"
                          Text="{x:Static resources:Labels.LoadGame}"
                          Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                          VerticalAlignment="Center"/>
                
                <Button Grid.Column="1" 
                       Style="{StaticResource MaterialDesignIconButton}" 
                       Command="{Binding CloseCommand}"
                       Margin="8">
                    <materialDesign:PackIcon Kind="Close"/>
                </Button>
            </Grid>

            <!-- Game List -->
            <Grid Grid.Row="1" 
                  Margin="0,8,0,8">
                <!-- Loading Progress Bar -->
                <ProgressBar IsIndeterminate="True"
                            Style="{StaticResource MaterialDesignLinearProgressBar}"
                            VerticalAlignment="Top"
                            Height="4"
                            Visibility="{Binding IsLoading, Converter={StaticResource BoolConverter}}"/>

                <!-- Games List -->
                <ListBox ItemsSource="{Binding Games}"
                        SelectedItem="{Binding SelectedGame}"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        Margin="0,4,0,0">

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.InputBindings>
                                    <MouseBinding MouseAction="LeftDoubleClick" 
                                                 Command="{Binding DataContext.OpenCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"/>
                                </Grid.InputBindings>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Game Info -->
                                <StackPanel Grid.Column="0" 
                                           VerticalAlignment="Center"
                                           Margin="16,8">
                                    <TextBlock Text="{Binding Name}"
                                              Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                              FontWeight="Medium"/>
                                    <TextBlock Text="{Binding CreationDate, StringFormat={x:Static resources:Labels.CreatedDateFormat}}"
                                              Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                              Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
                                              Opacity="0.7"/>
                                </StackPanel>

                                <!-- Action Buttons -->
                                <StackPanel Grid.Column="1" 
                                           Orientation="Horizontal"
                                           VerticalAlignment="Center"
                                           Margin="8,8,8,8">
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Setter Property="Visibility" Value="Hidden"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>

                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>

                <!-- Empty State -->
                <StackPanel VerticalAlignment="Center" 
                           HorizontalAlignment="Center"
                           Visibility="{Binding Games.Count, Converter={StaticResource CountToVis}, ConverterParameter=invert}">
                    <materialDesign:PackIcon Kind="GamepadVariant" 
                                            Width="64" Height="64"
                                            Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
                                            HorizontalAlignment="Center"/>
                    <TextBlock Text="{x:Static resources:Labels.NoGamesFound}"
                              Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                              HorizontalAlignment="Center"
                              Margin="0,16,0,8"
                              Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                </StackPanel>
            </Grid>

            <!-- Action Buttons -->
            <Grid Margin="16" Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Open Button -->
                <Button Grid.Column="1"
                       Style="{StaticResource MaterialDesignRaisedButton}"
                       Command="{Binding OpenCommand}"
                       Margin="0,0,8,0"
                       IsEnabled="{Binding SelectedGame, Converter={StaticResource NotNullToBool}}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="FolderOpen" Width="18" Height="18" Margin="0,0,8,0"/>
                        <TextBlock Text="{x:Static resources:Labels.Open}"/>
                    </StackPanel>
                </Button>

                <!-- Close Button -->
                <Button Grid.Column="2"
                       Style="{StaticResource MaterialDesignOutlinedButton}"
                       Command="{Binding CloseCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Close" Width="18" Height="18" Margin="0,0,8,0"/>
                        <TextBlock Text="{x:Static resources:Labels.Close}"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Grid>
    </materialDesign:Card>
</UserControl>