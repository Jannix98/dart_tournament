<UserControl x:Class="DartTournament.WPF.Controls.Toolbar.ToolbarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DartTournament.WPF.Controls.Toolbar"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:resx="clr-namespace:DartTournament.WPF.Resources"
             mc:Ignorable="d" 
             d:DesignHeight="62" d:DesignWidth="800" d:Background="White">
    <UserControl.Resources>
    </UserControl.Resources>
    <Grid Background="{DynamicResource MaterialDesign.Brush.ToolBar.Background}">
        <Grid  >
            <!-- Surface Container-->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" HorizontalAlignment="Center" Margin="0, 8, 0,8">
                <Image Source="pack://application:,,,/DartTournament.WPF;component/Assets/Icons/fackel.png" 
               Width="120" 
               Height="46" 
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               RenderOptions.BitmapScalingMode="HighQuality"/>
            </Grid>

            <ToolBarTray Grid.Row="0" Grid.Column="1" VerticalAlignment="Center">
                <ToolBar ToolBarTray.IsLocked="True" Margin="0">
                    <Button Click="AddGameBtn_Click"  Content="{materialDesign:PackIcon Kind=Plus}"/>
                    <Button Click="LoadGamesBtn_Click" Content="{materialDesign:PackIcon Kind=FolderOpen}"/>
                    <Button Click="PeopleBtn_Click" Content="{materialDesign:PackIcon Kind=Person}"/>
                </ToolBar>
            </ToolBarTray>
            <ToolBarTray Grid.Row="0" Grid.Column="2" VerticalAlignment="Center">
                <ToolBar ToolBarTray.IsLocked="True">
                    <Button Click="SettingsBtn_Click" Content="{materialDesign:PackIcon Kind=Settings}" HorizontalAlignment="Right"/>
                </ToolBar>
            </ToolBarTray>
            <!--<StackPanel Grid.Row="0" Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Left">
                <Button x:Name="SettingsBtn" Style="{StaticResource ToolbarButtonStyle}" Margin="0,0,5,0" Click="SettingsBtn_Click">
                    <materialDesign:PackIcon
                Kind="Settings"/>
                </Button>
            </StackPanel>-->
        </Grid>
    </Grid>
</UserControl>
