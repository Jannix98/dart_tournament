<UserControl x:Class="DartTournament.WPF.Controls.Toolbar.ToolbarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DartTournament.WPF.Controls.Toolbar"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:resx="clr-namespace:DartTournament.WPF.Resources"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="400" d:Background="White">
    <UserControl.Resources>
        <Style x:Key="ToolbarButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignFloatingActionMiniButton}">
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <!-- IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" -->
            <Setter Property="Margin" Value="15,0,0,0"/>
            <Setter Property="IsEnabled" Value="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"/>
        </Style>
        <Style TargetType="materialDesign:PackIcon">
            <Setter Property="Width" Value="18"/>
            <Setter Property="Height" Value="18"/>
        </Style>
    </UserControl.Resources>
    <Grid Background="{DynamicResource MaterialDesign.Brush.Primary.Dark}">
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button x:Name="AddGameBtn" Style="{StaticResource ToolbarButtonStyle}" Margin="5,0,0,0" Click="AddGameBtn_Click">
                <materialDesign:PackIcon
                    Kind="Plus"/>
            </Button>
            <Button x:Name="LoadGamesBtn" Style="{StaticResource ToolbarButtonStyle}" Click="LoadGamesBtn_Click">
                <materialDesign:PackIcon
                    Kind="FolderOpen"/>
            </Button>
            <Button x:Name="PeopleBtn" Style="{StaticResource ToolbarButtonStyle}" Click="PeopleBtn_Click">
                <materialDesign:PackIcon
                    Kind="Person"/>
            </Button>
            

        </StackPanel>
        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button x:Name="SettingsBtn" Style="{StaticResource ToolbarButtonStyle}" Margin="0,0,5,0" Click="SettingsBtn_Click">
                <materialDesign:PackIcon
                    Kind="Settings"/>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
