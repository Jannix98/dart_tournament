<UserControl x:Class="DartTournament.WPF.Controls.Game.GameMenue.GameMenueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DartTournament.WPF.Controls.Game.GameMenue"
             xmlns:resx="clr-namespace:DartTournament.WPF.Resources"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <Button Content="{x:Static resx:Labels.NewGame}"
                    Width="300"
                    Height="30"
                    Click="Button_Click_1" />
            <Button Content="{x:Static resx:Labels.LoadGame}"
                    Width="300"
                    Margin="0, 16, 0, 16"
                    Height="30"
                    Click="Button_Click" />
            <ContentControl x:Name="mainContent" />
        </StackPanel>
    </Grid>
</UserControl>
