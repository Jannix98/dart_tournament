<UserControl x:Class="DartTournament.WPF.Controls.PlayerOverview.PlayerOverviewDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DartTournament.WPF.Controls.PlayerOverview"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:resources="clr-namespace:DartTournament.WPF.Resources"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <materialDesign:Card MaxWidth="900" 
                         MaxHeight="600">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                          Text="{x:Static resources:Labels.PlayerManagement}" 
                          Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                          Margin="24,16"/>

                <Button Grid.Column="1" 
                       Style="{StaticResource MaterialDesignIconButton}" 
                       Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                       Margin="8">
                    <materialDesign:PackIcon Kind="Close"/>
                </Button>
            </Grid>

            <!-- Content -->
            <local:PlayerOverviewView Grid.Row="1" Margin="8"/>

            <!-- Footer -->
            <StackPanel Grid.Row="2" 
                       Orientation="Horizontal" 
                       HorizontalAlignment="Right" 
                       Margin="16">
                <Button Content="{x:Static resources:Labels.Close}" 
                       Margin="8,0" 
                       Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                       Style="{StaticResource MaterialDesignFlatButton}"/>
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</UserControl>