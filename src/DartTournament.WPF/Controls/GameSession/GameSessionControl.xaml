<UserControl x:Class="DartTournament.WPF.Controls.GameSession.GameSessionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DartTournament.WPF.Controls.GameSession"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" x:Name="TitleTextBlock"
               FontSize="24" FontWeight="Bold" Margin="10"/>
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <ContentControl x:Name="GameContent"/>    
        </ScrollViewer>
        
        <TabControl Grid.Row="1" x:Name="TabControl" Visibility="Collapsed">
            <TabItem x:Name="GameTab" Header="Game">
                <!-- Game tab content here -->
            </TabItem>
            <TabItem x:Name="LooserRoundTab" Header="Looser Round">
                <!-- Looser Round tab content here -->
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
