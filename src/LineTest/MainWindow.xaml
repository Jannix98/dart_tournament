<Window x:Class="LineTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LineTest"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="1000"
        Width="1000">
    <!--<Grid Name="MyGrid">
        --><!-- Spalten- und Zeilen-Definitionen --><!--
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        --><!-- Start-Button (links oben) -->
        <!--<Button Name="Button1"
                Content="Start"
                Grid.Row="0"
                Grid.Column="0" />
        <Button Name="Button2"
                Content="Ziel"
                Grid.Row="1"
                Grid.Column="1" />--><!--

        <Line X1="0"
              Y1="0"
              X2="1000"
              Y2="1000"
              Stroke="Red"
              
              StrokeThickness="2" />
    </Grid>-->
    <Canvas>
        <Line X1="0"
              Y1="0"
              X2="1000"
              Y2="1000"
              Stroke="Red"
              StrokeThickness="2" />
    </Canvas>
</Window>
